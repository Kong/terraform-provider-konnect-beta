// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package shared

import (
	"errors"
	"fmt"
	"github.com/kong/terraform-provider-konnect-beta/internal/sdk/internal/utils"
)

// Type - Field input type at submission time.
type Type string

const (
	TypeText     Type = "text"
	TypeEmail    Type = "email"
	TypeNumber   Type = "number"
	TypeTextarea Type = "textarea"
	TypeSelect   Type = "select"
	TypeCheckbox Type = "checkbox"
)

func (e Type) ToPointer() *Type {
	return &e
}

// IsExact returns true if the value matches a known enum value, false otherwise.
func (e *Type) IsExact() bool {
	if e != nil {
		switch *e {
		case "text", "email", "number", "textarea", "select", "checkbox":
			return true
		}
	}
	return false
}

type ValueType string

const (
	ValueTypeStr        ValueType = "str"
	ValueTypeNumber     ValueType = "number"
	ValueTypeBoolean    ValueType = "boolean"
	ValueTypeArrayOfStr ValueType = "arrayOfStr"
)

// Value - Submitted value. Type matches the field `type`: `text`/`email`/`textarea` return strings; `number` returns a number; `checkbox` returns a boolean; `select` returns a string for single-select or an array of strings for multi-select.
type Value struct {
	Str        *string  `queryParam:"inline" union:"member"`
	Number     *float64 `queryParam:"inline" union:"member"`
	Boolean    *bool    `queryParam:"inline" union:"member"`
	ArrayOfStr []string `queryParam:"inline" union:"member"`

	Type ValueType
}

func CreateValueStr(str string) Value {
	typ := ValueTypeStr

	return Value{
		Str:  &str,
		Type: typ,
	}
}

func CreateValueNumber(number float64) Value {
	typ := ValueTypeNumber

	return Value{
		Number: &number,
		Type:   typ,
	}
}

func CreateValueBoolean(boolean bool) Value {
	typ := ValueTypeBoolean

	return Value{
		Boolean: &boolean,
		Type:    typ,
	}
}

func CreateValueArrayOfStr(arrayOfStr []string) Value {
	typ := ValueTypeArrayOfStr

	return Value{
		ArrayOfStr: arrayOfStr,
		Type:       typ,
	}
}

func (u *Value) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  ValueTypeStr,
			Value: &str,
		})
	}

	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  ValueTypeNumber,
			Value: &number,
		})
	}

	var boolean bool = false
	if err := utils.UnmarshalJSON(data, &boolean, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  ValueTypeBoolean,
			Value: &boolean,
		})
	}

	var arrayOfStr []string = []string{}
	if err := utils.UnmarshalJSON(data, &arrayOfStr, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  ValueTypeArrayOfStr,
			Value: arrayOfStr,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for Value", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for Value", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(ValueType)
	switch best.Type {
	case ValueTypeStr:
		u.Str = best.Value.(*string)
		return nil
	case ValueTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case ValueTypeBoolean:
		u.Boolean = best.Value.(*bool)
		return nil
	case ValueTypeArrayOfStr:
		u.ArrayOfStr = best.Value.([]string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for Value", string(data))
}

func (u Value) MarshalJSON() ([]byte, error) {
	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Boolean != nil {
		return utils.MarshalJSON(u.Boolean, "", true)
	}

	if u.ArrayOfStr != nil {
		return utils.MarshalJSON(u.ArrayOfStr, "", true)
	}

	return nil, errors.New("could not marshal union type Value: all fields are null")
}

// FormResponseEntry - A single field's submitted answer, snapshotted with the field metadata as it appeared at submission time.
type FormResponseEntry struct {
	// Field slug, matches the parent object key.
	Name string `json:"name"`
	// Field label snapshotted at submission time.
	Label string `json:"label"`
	// Field input type at submission time.
	Type Type `json:"type"`
	// Submitted value. Type matches the field `type`: `text`/`email`/`textarea` return strings; `number` returns a number; `checkbox` returns a boolean; `select` returns a string for single-select or an array of strings for multi-select.
	//
	Value Value `json:"value"`
}

func (f *FormResponseEntry) GetName() string {
	if f == nil {
		return ""
	}
	return f.Name
}

func (f *FormResponseEntry) GetLabel() string {
	if f == nil {
		return ""
	}
	return f.Label
}

func (f *FormResponseEntry) GetType() Type {
	if f == nil {
		return Type("")
	}
	return f.Type
}

func (f *FormResponseEntry) GetValue() Value {
	if f == nil {
		return Value{}
	}
	return f.Value
}
